name: CI

on:
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Test & Build
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: genesis-web/package-lock.json

      - name: Install dependencies
        working-directory: genesis-web
        run: npm ci

      - name: Run tests
        working-directory: genesis-web
        run: npm run test

      - name: Type-check and build
        working-directory: genesis-web
        run: npm run build

      # Text layout needs a real browser — jsdom has no layout engine, so three
      # separate display strings shipped wrapped before this check existed.
      - name: Install Chromium for layout checks
        working-directory: genesis-web
        run: npx playwright install --with-deps chromium

      - name: Check text layout
        working-directory: genesis-web
        run: npm run validate:layout
